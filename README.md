David Dossett's personal website, built with Astro and deployed as a static website.

## Development

```sh
npm install
npm run dev
```

Run `npm run lint` to check the project and `npm run build` to create the
production site in `dist/`.